Run ruff check with auto-fix and ruff format across the entire backend directory.

Execute these commands:
1. `cd backend && ruff check --fix .` - Check and auto-fix linting issues
2. `cd backend && ruff format .` - Format all Python files

Run both commands and report any issues that couldn't be auto-fixed.
